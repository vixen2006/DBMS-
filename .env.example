# PostgreSQL Connection String (Format: postgresql://user:password@host:port/database)
# For Render, copy the "External Database URL" from your Render PostgreSQL dashboard
DATABASE_URL=postgresql://postgres:yourpassword@localhost:5432/online_music_shop

# Session Secret (Optional)
SESSION_SECRET=my_super_secret_key
